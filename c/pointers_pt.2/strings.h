#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>

#pragma once

void SwapSizeT(size_t* a, size_t* b);
void SwapPointers(size_t** ptr1, size_t** ptr2);
void TestStrCmp();
